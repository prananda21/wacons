{
  "name": "wacons",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "be:dev": "bun run --cwd backend dev",
    "fe:dev": "bun run --cwd frontend dev",
    "dev": "bun run be:dev & bun run fe:dev",
    "build:fe": "bun run --cwd frontend build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
